# Lambda TODO

## メール送信処理の修正

- [ ] `sendNotificationIfNeeded` の未使用引数 `now time.Time` を削除する
- [ ] `daysUntilDue` の計算を時間ベース（`Hours() / 24`）から日付ベースに修正する（端数による誤判定を防ぐ）
- [ ] SES 送信失敗時のエラーログに送信先メールアドレスを含める

## テストデータの作成

- [ ] テスト用ユーザーの INSERT SQL を作成する
- [ ] 期日間近（3日以内）の TODO データを作成する
- [ ] 期日超過の TODO データを作成する
- [ ] 通知済みレコード（重複防止確認用）のデータを作成する
